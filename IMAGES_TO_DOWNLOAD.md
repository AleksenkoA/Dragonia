# 🖼️ Изображения для скачивания с dragonia.com

## 🔴 КРИТИЧНО (обязательно для сдачи):

### 1. Логотип
- **Файл:** `logo.webp` или `logo.png`
- **Где найти:** Верхний левый угол сайта (в sidebar)
- **Размер:** ~200x60px
- **Путь в проекте:** `assets/images/logo.webp`

### 2. Welcome Bonus Баннер
- **Файл:** `welcome-banner.webp` или `hero-banner.webp`
- **Где найти:** Главный баннер на странице (с текстом "Grab €300 with Bonus Crab!" или похожий)
- **Размер:** ~1200x400px
- **Путь в проекте:** `assets/images/welcome-banner.webp`

---

## 🟡 ВАЖНО (для качества):

### 3. Иконки боковой панели (можно скачать по одной):

**BONUS CRAB:**
- Ищите в sidebar, рядом с текстом "BONUS CRAB"
- Сохранить как: `bonus-crab-icon.png`

**WHEEL OF FORTUNE:**
- Ищите в sidebar, рядом с текстом "WHEEL OF FORTUNE"
- Сохранить как: `wheel-of-fortune-icon.png`

**CHALLENGES:**
- Ищите в sidebar, рядом с текстом "CHALLENGES"
- Сохранить как: `challenges-icon.png`

**SHOP:**
- Ищите в sidebar, рядом с текстом "SHOP"
- Сохранить как: `shop-icon.png`

**VIP LEVELS:**
- Ищите в sidebar, рядом с текстом "VIP LEVELS"
- Сохранить как: `vip-levels-icon.png`

**CASINO:**
- Ищите в sidebar, рядом с текстом "CASINO"
- Сохранить как: `casino-icon.png`

**JACKPOTS:**
- Ищите в sidebar, рядом с текстом "JACKPOTS"
- Сохранить как: `jackpots-icon.png`

**SPORTS:**
- Ищите в sidebar, рядом с текстом "SPORTS"
- Сохранить как: `sports-icon.png`

**PROMOTIONS, TOURNAMENTS, SPIN RALLY, LIVE CASINO:**
- Уже есть в проекте, но можно обновить на более качественные версии

---

## 📥 КАК СКАЧАТЬ:

### Метод 1: Через DevTools (рекомендуется)

1. Откройте https://dragonia.com/en/ в Chrome
2. Нажмите **F12** (или Cmd+Option+I на Mac)
3. Перейдите на вкладку **Network**
4. В фильтре выберите **Img** (только изображения)
5. Обновите страницу (**F5**)
6. В списке найдите нужное изображение
7. Правый клик → **Open in new tab**
8. Сохраните изображение (Cmd+S / Ctrl+S)
9. Переименуйте и переместите в `assets/images/`

### Метод 2: Прямой экспорт из элементов

1. Откройте https://dragonia.com/en/
2. Нажмите **F12** → вкладка **Elements**
3. Найдите элемент с изображением (например, логотип)
4. Правый клик на `<img>` → **Copy image address**
5. Откройте ссылку в новой вкладке
6. Сохраните изображение

### Метод 3: Через консоль браузера

Откройте консоль (F12 → Console) и выполните:

```javascript
// Найти все изображения
document.querySelectorAll('img').forEach((img, i) => {
    console.log(`${i+1}. ${img.src}`);
    console.log(`   Alt: ${img.alt || 'no alt'}`);
    console.log(`   Class: ${img.className || 'no class'}`);
    console.log('---');
});

// Найти логотип
const logo = document.querySelector('img[src*="logo" i]');
if (logo) {
    console.log('Logo found:', logo.src);
    window.open(logo.src, '_blank');
}
```

---

## ✅ ПОСЛЕ СКАЧИВАНИЯ:

1. Сохраните все изображения в `assets/images/`
2. Убедитесь, что имена файлов совпадают с теми, что используются в HTML
3. Обновите версионирование в HTML: замените `?v=4` на `?v=5`
4. Проверьте сайт в браузере
5. Убедитесь, что нет битых изображений (404 ошибок)

---

## 📝 ПРИМЕЧАНИЯ:

- Если не можете найти конкретное изображение, можно оставить эмодзи как временное решение
- Логотип и Welcome Banner - самые важные, их нужно скачать обязательно
- Остальные иконки можно скачать постепенно
